# Normalize line endings to LF so diffs are deterministic across platforms.
# This matters for the pipeline's committed data files especially.
* text=auto eol=lf

# Binary assets — never touch these.
*.webp binary
*.woff2 binary
*.png binary
*.jpg binary
*.ico binary
